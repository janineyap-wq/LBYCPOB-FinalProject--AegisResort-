# Server Configuration
server.port=8080

# Application Name
spring.application.name=aegis-resort-system

# H2 Database Connection Settings
spring.datasource.url=jdbc:h2:mem:aegisdb;DB_CLOSE_DELAY=-1;DB_CLOSE_ON_EXIT=FALSE
spring.datasource.driverClassName=org.h2.Driver
spring.datasource.username=sa
spring.datasource.password=

# JPA / Hibernate Configuration
spring.jpa.database-platform=org.hibernate.dialect.H2Dialect
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true

# H2 Console Settings (Access at http://localhost:8080/h2-console)
spring.h2.console.enabled=true
spring.h2.console.path=/h2-console

# Thymeleaf Cache (Disabled for local development hot-reloading)
spring.thymeleaf.cache=false